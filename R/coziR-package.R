#' coziR: Conditional Neighborhood Preference Scores
#'
#' `coziR` implements a COZI-style workflow for cell neighborhood analysis:
#' 1. Build neighborhood graph (`knn`, `radius`, or `delaunay`).
#' 2. Quantify directional type-to-type neighbor interactions.
#' 3. Apply conditional normalization.
#' 4. Compute permutation-based z-scores.
#' 5. Return conditional cell ratios and z-scores.
#'
#' @docType package
#' @name coziR
NULL
